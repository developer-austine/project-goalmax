/* ============================latest project nav buttons press */

// let  = document.querySelectorAll('.slide-container');
// let index = 0;

// next function
// function next(){
//   slides[index].classList.remove('active');
//   index = (index + 1)% slides.length;
//   slides[index].classList.add('active');
// }

// previous function
// function prev(){
//   slides[index].classList.remove('active');
//   index = (index - 1 + slides.length)% slides.length;
//   slides[index].classList.add('active');
// }

// auto-play

// setInterval(next, 5000);//slide chnages every 5secs

/* =================slider section design for patners */
/* const slides = document.querySelectorAll('.pics img');
const slideCount = slides.length;
let cuurentSlide = 0;

function showSlide(){
  slides.forEach((slide) => {
    slide.style.display = 'none';
  });
  for(let i = currentSlide; i < currentSlide +3; i++) {
    if (i < slideCount) {
      slides[i].style.display = 'block';
    }
  }
  currentSlide += 3;
  if (currentSlide >= slideCount) {
    currentSlide = 0;
  }
  setTimeout(showSlide, 7000);
}

showSlide();
 */